name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      SOURCE_DATE_EPOCH: "1700000000"
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.13"
      - run: python -m pip wheel . --no-deps --wheel-dir dist
      - run: python scripts/verify_release.py --tag "${GITHUB_REF_NAME}" --wheel dist/codebase_atlas-*.whl
      - run: python scripts/check_publication_readiness.py --mode public
      - name: Write checksum
        run: cd dist && sha256sum codebase_atlas-*.whl > SHA256SUMS.txt
      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "${GITHUB_REF_NAME}" dist/* --verify-tag --generate-notes --draft
